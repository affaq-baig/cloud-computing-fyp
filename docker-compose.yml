services:
  myweb-dev:
    image: nginx:latest
    container_name: myweb-dev
    ports:
      - "2200:80"
    volumes:
      - .:/usr/share/nginx/html
    networks:
      - myapp-net
 

networks:
  myapp-net:
    driver: bridge